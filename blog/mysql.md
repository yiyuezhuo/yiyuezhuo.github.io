首先作死在下了百度挂在首页的mysql，居然是“绿色版”，用不了。

跑到mysql官网又下了一遍装，作死换了端口号（因为显示已使用）和关掉了开机自动启动sql服务。结果又浪费了时间去解决跑到服务去手动开服务的破事。换端口号则更加坑爹，明明菜单里那个可以运行但bin那个却不能运行，说是有个my.ini的配置文件说的目录哪都找不到，后来又看一篇文章才发现快捷方式自己重定位了那个配置文件，而配置文件的各个目录反而却根本没有这个文件。结果又到那个和安装目录根本没关系的所谓数据目录找到my.ini（这个文件已经在安装时根据我的指示修改了默认端口号）又复制到搜索路径下才算能打开bin下的那个mysql（这货还不吃help，根本查不到它有什么可以用来配置的命令（事实上它用的貌似- --长命令(菜单里那个快捷方式显示的)，而我只试了\/？直接输几种，还是too young））